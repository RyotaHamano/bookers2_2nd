 <% if current_user.follow?(user) %>
  <%= link_to 'フォロー外す', user_relations_path(user), method: :delete, remote: true, class: 'btn btn-danger' %>
<% else %>
  <%= link_to 'フォローする', user_relations_path(user), method: :post, remote: true, class: 'btn btn-primary'%>
<% end %>