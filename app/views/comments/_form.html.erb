 <%= form_with model: @comment, url: book_comments_path(@book.id), local: false do |f| %>
  <div class="form-group">
    <p>Comment</p>
    <%= f.text_area :comment %>
  </div>
  <div class="form-group">
    <%= f.submit '送信', class:'btn btn-success' %>
  </div>
<% end %>